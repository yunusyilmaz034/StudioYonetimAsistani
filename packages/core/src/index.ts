// @studio/core — the product. Framework-free domain and application code.
//
// This is the package's only public door (AD-29). Nothing is exported yet:
// the shared kernel (ids, money, time, actor, event envelope, TenantContext,
// Clock) and the domain modules arrive in the next milestone (Doc 8, Day 1 PM
// and Day 2). This file exists so the toolchain has a compilation root and the
// `pnpm check` gate is green on an otherwise empty package — which is exactly
// what Doc 8 Day 1 PM asks the scaffold to prove.
export {}
